<h1>Edit Occasion</h1>
<br>
<%= flash[:message] %>
<br>
<%= form_for(@occasion) do |f| %>
  <%= f.label "Title:" %>
  <%= f.text_field :title %><br>
  <%= f.fields_for :term, @occasion.term do |t| %>
    <%= t.label "Start Time:" %>
    <%= t.datetime_field :start_term, min: DateTime.now %><br>
    <%= t.label "End Time:" %>
    <%= t.datetime_field :end_term, min: DateTime.now %><br>
  <% end %>
  <%= f.submit %>
<% end %>
